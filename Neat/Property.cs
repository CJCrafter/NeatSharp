namespace Neat {
    public enum Property {
        SPECIES_DISTANCE,
        EXCESS_FACTOR,
        DISJOINT_FACTOR,
        WEIGHT_FACTOR,
        
        RANDOM_WEIGHT_STRENGTH,
        SHIFT_WEIGHT_STRENGTH,

        MUTATE_RANDOM_WEIGHT,
        MUTATE_SHIFT_WEIGHT,
        MUTATE_TOGGLE_LINK,
        MUTATE_LINK,
        MUTATE_NODE,
        MUTATE_REMOVE_NODE,
        
        SURVIVAL_CHANCE
    }
}